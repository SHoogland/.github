name: Renovate

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_call: 
  workflow_dispatch:

jobs:
  renovate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: shoogland/.github/renovate-runner@main
